# homebrew-cmdv

Homebrew Cask tap for [CmdV](https://github.com/ddhgo/CmdV) — macOS clipboard history manager.

## Install

```bash
brew tap ddhgo/cmdv
brew install --cask cmdv
```

## Uninstall

```bash
brew uninstall --cask cmdv
```
